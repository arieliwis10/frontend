import { Injectable } from '@angular/core';

/**
 * Servicio general para funcionalidades compartidas o utilitarias.
 * Puedes agregar aquí métodos reutilizables para toda la aplicación.
 */
@Injectable({
  providedIn: 'root'
})
export class ServiciosService {
  /**
   * Constructor del servicio ServiciosService.
   * Puedes inyectar dependencias aquí si es necesario.
   */
  constructor() { }
}
